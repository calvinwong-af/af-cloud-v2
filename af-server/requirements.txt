# AF Server V2 — FastAPI / Cloud Run
# Replaces: Flask 2.0.1 / Python 3.7 / GAE
#
# Pinned to versions with pre-built Windows wheels.
# pydantic v1 (pure Python — no Rust/Cargo required).

# Web framework
fastapi==0.103.2
uvicorn[standard]==0.23.2

# Pydantic v1 — pure Python, no Rust compiler needed
pydantic==1.10.13

# Google Cloud
google-cloud-datastore==2.19.0
google-cloud-storage==2.18.2
firebase-admin==6.5.0

# HTTP client (for any outbound calls e.g. Mailgun, Xero)
httpx==0.25.2

# Utilities
python-dotenv==1.0.1
