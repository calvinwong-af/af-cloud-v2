# AF Server V2 — FastAPI / Cloud Run
# Database: Cloud SQL (PostgreSQL 15) via SQLAlchemy Core + Cloud SQL Python Connector

# Web framework
fastapi==0.103.2
uvicorn[standard]==0.23.2

# Pydantic v1
pydantic==1.10.13

# Database — Cloud SQL Python Connector (pg8000, no binary deps on Cloud Run)
cloud-sql-python-connector[pg8000]==1.10.0
sqlalchemy==2.0.30
pg8000==1.31.2

# Local dev only — psycopg2 for direct TCP connection
psycopg2-binary==2.9.9

# Google Cloud (retain for GCS and Firebase)
google-cloud-storage==2.18.2
firebase-admin==6.5.0

# HTTP client
httpx==0.25.2

# AI (BL parsing)
anthropic>=0.49.0

# Multipart file uploads
python-multipart==0.0.9

# Utilities
python-dotenv==1.0.1
